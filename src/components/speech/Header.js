import React, { Component } from 'react';
import {
  Text,
  Image,
  View,
  TouchableOpacity,
  AsyncStorage
} from 'react-native';

// Contains sentence display.
// Needs stuff from parent component, SpeechApp
// Has complete

export default class Header extends Component {

  constructor(props) {
    super(props)
  }

// has sentence bar. Card display. Delete button.
// potentially settings toggle.
// Card display has state passed down from parent.
  render() {

  }

}
